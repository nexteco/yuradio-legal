# YuRadio 개인정보처리방침

**최종 수정일**: 2026년 8월 10일
**적용 대상**: YuRadio Android 앱 (패키지명 `kr.nexteco.yuradio`)

NexTeco(이하 "개발자")는 YuRadio 앱 사용자의 개인정보를 중요하게 생각하며, 관련 법령을 준수합니다. 본 방침은 앱이 어떤 정보를 수집·사용·공유하는지 설명합니다.

## 1. 수집하는 정보

본 앱은 **계정 가입·로그인을 요구하지 않으며**, 사용자를 식별할 수 있는 개인정보(이름, 이메일, 전화번호 등)를 수집하지 않습니다.

다음 데이터만 제한적으로 사용됩니다.

### 1.1 기기에 로컬로 저장되는 데이터

- 즐겨찾기 방송국 목록 (이름, URL, 주파수, 태그)
- 선택한 국가 프리셋
- 앱 UI 설정 (언어, 정렬 순서 등)
- 마지막에 재생한 방송국 ID (앱 재실행 시 자동 복원용)
- 데이터 사용량 통계 (일자별 통신량, Wi-Fi/모바일 구분)
- 익명 설치 식별자 (앱 최초 실행 시 생성되는 임의의 난수 값 — 아래 1.4 참고)

식별된 곡 정보는 화면 표시용으로만 메모리에 두며, 채널을 바꾸거나 앱을 종료하면
사라집니다. 별도로 기록을 남기지 않습니다.

이 데이터는 **전부 기기 내부 SQLite 데이터베이스에만 저장되며, 외부로 전송되지 않습니다.**

### 1.2 네트워크를 통해 송수신되는 데이터

앱 기능 수행을 위해 다음 외부 서버와 통신합니다.

| 외부 서비스 | 전송 내용 | 목적 |
|---|---|---|
| Radio Browser (radio-browser.info) | 국가 코드, 검색어 | 방송국 목록 조회 |
| 각 방송사 스트리밍 서버 | HTTP 요청 | 라디오 오디오 재생 |
| AcoustID (acoustid.org) | 오디오 지문(fingerprint), 길이 정보 | 곡 자동 식별 (옵션) |
| MusicBrainz (연계) | recording ID | 식별된 곡 메타데이터 |
| 방송사 선곡 정보 API (MBC, SBS) | HTTP 요청 | 현재 방송 중인 곡·프로그램 표시 (옵션) |
| 청취 현황 집계 서버 (Cloudflare) | 익명 설치 식별자, 앱 버전 | 지역별 청취 현황 집계 (옵션, 아래 1.4) |

**오디오 지문**은 약 16초 분량 오디오의 특징을 추출한 짧은 해시값으로, **원본 음성을 복원할 수 없습니다.** 자동 식별 기능이 꺼져 있으면 전송되지 않습니다.

### 1.4 익명 청취 현황 집계

앱은 어느 지역에서 얼마나 듣고 있는지 집계해 공개 지도에 표시합니다.

**보내는 것**은 앱 최초 실행 시 만들어진 **임의의 난수 식별자**(설치 ID)와 **앱 버전**
뿐입니다. 기기 고유번호나 광고 식별자가 아니며, 앱을 삭제 후 다시 설치하면 새 값이
만들어집니다. **위치 정보(GPS)나 듣고 있는 채널은 보내지 않습니다.**

**서버는 접속 IP 주소로부터 국가와 광역 행정구역(시·도 단위)만 추정**합니다.
시·군·구 이하는 정확도가 낮아 수집하지 않으며, 지도에 표시하는 좌표는 약 11km
단위로 반올림합니다. **원본 IP 주소는 저장하지 않습니다.**

방송을 재생 중일 때만 약 5분 간격으로 전송하며, 재생을 멈추거나 앱을 종료하면
집계에서 즉시 제외됩니다.

**보관 기간**

| 구분 | 기간 |
|---|---|
| 현재 청취 세션 | 마지막 신호 후 24시간 이내 자동 삭제 |
| 설치 식별자 기록 (이용자·설치자 수 집계용) | 마지막 접속 후 400일 |
| 일자별 지역 집계 (개인 식별 불가) | 400일 |

**끄는 방법**: 앱 → 설정 → 정보 탭 → 청취 현황 → **익명 청취 현황 집계** 스위치를
끄면 전송이 즉시 중단되고 서버의 현재 세션 기록도 바로 삭제됩니다.

이 집계는 Cloudflare, Inc.의 서버(Cloudflare Workers 및 D1)에서 처리·보관됩니다.

### 1.3 시스템 권한

| 권한 | 용도 | 필수/선택 |
|---|---|---|
| `INTERNET` | 라디오 스트림 다운로드 및 API 호출 | 필수 |
| `FOREGROUND_SERVICE` / `FOREGROUND_SERVICE_MEDIA_PLAYBACK` | 백그라운드 재생 | 필수 |
| `WAKE_LOCK` | 재생 중 화면 꺼져도 오디오 유지 | 필수 |

접근이 불필요한 위치·연락처·사진·마이크 등의 권한은 **요청하지 않습니다**.

## 2. 정보 공유

개발자는 사용자 데이터를 제3자에게 판매·대여·공유하지 않습니다.

앱 동작상 필요한 경우에만 위 1.2의 외부 서비스와 통신하며, 이때 각 서비스는 해당 서비스의 자체 정책에 따라 트래픽을 처리합니다.

- Radio Browser 정책: https://www.radio-browser.info/
- AcoustID 정책: https://acoustid.org/privacy
- MusicBrainz 정책: https://metabrainz.org/privacy
- Cloudflare 정책: https://www.cloudflare.com/privacypolicy/

## 3. 광고·분석·크래시 리포팅

- **광고 SDK 미포함**
- **분석(Analytics) SDK 미포함** (Firebase Analytics, Google Analytics 등)
- **크래시 리포팅 SDK 미포함** (Crashlytics, Sentry 등)

제3자 분석 도구는 사용하지 않으나, 위 1.4의 자체 청취 현황 집계는 예외이며
설정에서 끌 수 있습니다.

## 4. 어린이 관련

본 앱은 13세 미만 아동을 대상으로 하지 않으며, 대상 연령층은 **전체 이용가**이지만 보호자 감독하에 사용을 권장합니다.

## 5. 데이터 삭제

**기기 내 데이터**는 앱을 삭제하면 모두 함께 제거됩니다. 개별 항목은 앱 안에서
지울 수 있습니다.

- 즐겨찾기 방송국: 설정 → 채널 목록에서 삭제, 또는 목록에서 길게 눌러 삭제
- 데이터 사용량 기록: 설정 → 정보 → 사용량 초기화
- 전체 초기화: 안드로이드 설정 → 앱 → YuRadio → 저장공간 → 데이터 삭제

**서버 측 데이터**는 위 1.4의 청취 현황 집계뿐이며, 다음과 같이 처리됩니다.

- 설정 → 정보 탭 → 청취 현황 → 익명 청취 현황 집계를 끄면 전송이 중단되고
  현재 세션 기록은 즉시 삭제됩니다
- 남는 기록은 익명 설치 식별자 기준의 접속 일자뿐이며, 위 보관 기간이 지나면
  자동으로 삭제됩니다
- 앱을 삭제하고 다시 설치하면 새 식별자가 만들어지므로 이전 기록과 연결되지 않습니다
- 그 밖의 삭제 요청은 아래 문의처로 연락해 주시기 바랍니다

## 6. 방침 변경

본 방침이 변경되는 경우 앱 업데이트 및 본 페이지에서 고지합니다.

## 7. 문의

개인정보 관련 문의는 아래로 연락주시기 바랍니다.

**NexTeco**
이메일: nexteco.kr@gmail.com
