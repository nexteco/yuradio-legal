# YuRadio 개인정보처리방침

**시행일**: 2026년 8월 12일
**최종 수정일**: 2026년 8월 12일
**적용 대상**: YuRadio Android 앱 (패키지명 `kr.nexteco.yuradio`)

NexTeco(이하 "개발자")는 YuRadio 앱 사용자의 개인정보를 중요하게 생각하며, 관련 법령을 준수합니다. 본 방침은 앱이 어떤 정보를 수집·사용·공유하는지 설명합니다.

## 요약

- **회원가입이 없습니다.** 이름, 이메일, 전화번호, 계정을 요구하지 않습니다.
- **개발자 서버로 보내는 정보가 없습니다.** 개발자는 이용자로부터 어떤 데이터도 수집·보관하지 않습니다.
- **위치 권한을 사용하지 않습니다.** GPS 좌표를 수집하지 않습니다.
- **마이크를 사용하지 않습니다.** 곡 식별은 앱이 재생 중인 방송 소리를 앱 내부에서 분석하는 방식이며, 주변 소리를 듣지 않습니다.
- **광고가 없고, 광고 식별자(AAID)를 사용하지 않으며, 분석·크래시 리포팅 SDK를 넣지 않았습니다.**
- 저장한 채널·설정은 **기기 안에만** 있습니다.

## 1. 수집하는 정보

본 앱은 **계정 가입·로그인을 요구하지 않으며**, 사용자를 식별할 수 있는 개인정보(이름, 이메일, 전화번호 등)를 수집하지 않습니다.

### 1.1 기기에 로컬로 저장되는 데이터

- 즐겨찾기 방송국 목록 (이름, URL, 주파수, 태그)
- 그룹, 선택한 국가 프리셋
- 앱 UI 설정 (언어, 정렬 순서, 자동 식별 켜기/끄기 등)
- 마지막에 재생한 방송국 ID (앱 재실행 시 자동 복원용)
- 데이터 사용량 통계 (일자별 통신량, Wi-Fi/모바일 구분)

식별된 곡 정보는 화면 표시용으로만 메모리에 두며, 채널을 바꾸거나 앱을 종료하면 사라집니다. 별도로 기록을 남기지 않습니다.

이 데이터는 **전부 기기 내부 SQLite 데이터베이스에만 저장되며, 외부로 전송되지 않습니다.** 앱을 삭제하면 함께 지워집니다.

### 1.2 네트워크를 통해 송수신되는 데이터

앱 기능 수행을 위해 다음 외부 서버와 통신합니다. **아래 서비스는 모두 제3자가 운영하며, 개발자가 운영하는 서버는 없습니다.** 인터넷 통신의 성격상 IP 주소와 접속 시각이 해당 서비스에 전달되며, 이는 각 서비스의 개인정보처리방침을 따릅니다.

| 외부 서비스 | 전송 내용 | 목적 |
|---|---|---|
| Radio Browser (radio-browser.info) | 국가 코드, 검색어, 재생한 방송국 식별자(인기 통계용) | 방송국 목록 조회 |
| 각 방송사 스트리밍 서버 (KBS·MBC·SBS·CBS 등) | HTTP 요청 | 라디오 오디오 재생 |
| AcoustID (acoustid.org) | 오디오 지문(fingerprint), 길이 정보 | 곡 자동 식별 (옵션) |
| MusicBrainz (연계) | recording ID | 식별된 곡 메타데이터 |
| 방송사 선곡 정보 API (MBC, SBS) | HTTP 요청 | 현재 방송 중인 곡·프로그램 표시 (옵션) |

**오디오 지문**은 약 16초 분량 오디오의 특징을 추출한 짧은 해시값으로, **원본 음성을 복원할 수 없습니다.**

**자동 식별을 끄면** 위 표의 AcoustID·MusicBrainz·방송사 선곡 정보 API 통신은 발생하지 않습니다 (앱 화면의 "자동 식별" 버튼).

### 1.3 개발자가 수집하는 정보

**없습니다.** 개발자는 이용자의 어떤 데이터도 서버로 수집하거나 보관하지 않습니다. 앱에는 개발자가 운영하는 수집 서버로 정보를 보내는 기능이 포함되어 있지 않습니다.

### 1.4 시스템 권한

| 권한 | 용도 | 필수/선택 |
|---|---|---|
| `INTERNET` | 라디오 스트림 다운로드 및 API 호출 | 필수 |
| `FOREGROUND_SERVICE` / `FOREGROUND_SERVICE_MEDIA_PLAYBACK` | 백그라운드 재생, 알림의 재생 버튼 | 필수 |
| `WAKE_LOCK` | 재생 중 화면이 꺼져도 연결 유지 | 필수 |

접근이 불필요한 위치·연락처·사진·마이크 등의 권한은 **요청하지 않습니다**.

## 2. 정보 공유

개발자는 수집하는 데이터가 없으므로, 제3자에게 판매·대여·공유할 사용자 데이터가 없습니다.

앱 동작상 필요한 경우에만 위 1.2의 외부 서비스와 통신하며, 이때 각 서비스는 해당 서비스의 자체 정책에 따라 트래픽을 처리합니다.

- Radio Browser 정책: https://www.radio-browser.info/
- AcoustID 정책: https://acoustid.org/privacy
- MusicBrainz 정책: https://metabrainz.org/privacy

## 3. 광고·분석·크래시 리포팅

- **광고 SDK 미포함**
- **분석(Analytics) SDK 미포함** (Firebase Analytics, Google Analytics 등)
- **크래시 리포팅 SDK 미포함** (Crashlytics, Sentry 등)
- **자체 분석·집계 서버 없음**

## 4. 어린이 관련

본 앱은 13세 미만 아동을 대상으로 하지 않으며, 대상 연령층은 **전체 이용가**이지만 보호자 감독하에 사용을 권장합니다.

## 5. 데이터 삭제

개발자가 보관하는 서버 측 데이터가 없으므로, 삭제 요청 대상이 되는 서버 기록도 없습니다.

**기기 내 데이터**는 앱을 삭제하면 모두 함께 제거됩니다. 개별 항목은 앱 안에서 지울 수 있습니다.

- 즐겨찾기 방송국: 설정 → 채널 목록에서 삭제, 또는 목록에서 길게 눌러 삭제
- 데이터 사용량 기록: 설정 → 정보 → 사용량 초기화
- 전체 초기화: 안드로이드 설정 → 앱 → YuRadio → 저장공간 → 데이터 삭제

## 6. 방침 변경

본 방침이 변경되는 경우 앱 업데이트 및 본 페이지에서 고지합니다. 향후 이용 통계 기능을 추가하는 경우, 해당 기능은 **기본으로 꺼진 상태(옵트인)** 로 제공하고 본 방침과 Play 스토어 데이터 세이프티 신고를 함께 갱신한 뒤에만 적용합니다.

## 7. 문의

개인정보 관련 문의는 아래로 연락주시기 바랍니다.

**NexTeco**
이메일: nexteco.kr@gmail.com

---

[English version](en.md)
